# 2026-05-18 1.2.0-operational-ux v1

## Objective

Reduce operational friction for Codex, Antigravity, Claude, MCP, and API/UI integrations without weakening safety.

## Files read

- `src/RagnaForge.Agent.Cli/Program.cs`
- `src/RagnaForge.Agent.Core/Commands/*`
- `src/RagnaForge.Agent.Core/Scanning/*`
- `src/RagnaForge.Agent.Mcp/*`
- `tests/RagnaForge.Agent.Core.Tests/*`
- `README.md`
- `docs/CLI.md`
- `docs/MCP.md`
- `docs/AI_AGENT_CONTRACT.md`
- `docs/RAGNAFORGE_AGENT.md`
- `docs/ROADMAP_AGENT.md`

## Files changed

- CLI command wiring for `baseline` and `health`
- core command set for `baseline`, `health`, config preflight, cache trust, and validation classification
- MCP tool registry and policy
- tests
- docs

## Preserved boundaries

- no real apply
- no real rollback
- no GRF modification
- no `.lub` editing
- no HTTP destructive surface

## Main decisions

1. Add `baseline` as the one-shot operational checkpoint.
2. Add `health` as the compact integration summary.
3. Keep external-data validation issues visible, but avoid using them to block read-only work by default.
4. Surface stale cache details explicitly instead of forcing callers to diff fingerprints manually.
5. Make `config set` reject bad paths before saving.

## Tests

- `dotnet build RagnaForge.Agent.slnx`
- `dotnet test RagnaForge.Agent.slnx`

## Result

- build passing
- `183/183` tests passing
- `baseline` and `health` available in CLI and MCP

## Remaining risks

- some older files still contain historical encoding artifacts outside the files rewritten in this pass
- installed `dist` artifacts were not repackaged in this round

## Recommended next step

Run a packaging-focused pass to refresh installed artifacts and verify the new CLI/MCP surface from `dist/agente-setimmo`.
